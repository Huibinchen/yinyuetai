import Vue from 'vue'
import Vuex from 'vuex'
// 引入axios
import http from 'axios'

Vue.use(Vuex)

const state = {
	
}

const actions = {
	
}

const mutations = {
	
}
export default new Vuex.Store({
  state,
  actions,
  mutations
})
